using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponDisplay : MonoBehaviour {
    /*public Weapon MyWeapon;
    public int MyNumber;
    public Weapon[] WeaponArray;
    public GlobalGameManager GMM;
    public TextMesh MyName;
	// Use this for initialization
	void Start ()
    {
        GMM = FindObjectOfType<GlobalGameManager>();
        WeaponArray = GMM.ShowRoomWeapons;
        MyWeapon = WeaponArray[MyNumber];
        if (MyWeapon != null)
        {
            Instantiate(MyWeapon, (gameObject.transform.position + new Vector3(0, 4, 0)), Quaternion.identity, gameObject.transform);
            MyName.text = MyWeapon.Name;
        }
    }
	
	// Update is called once per frame
	void Update () {
		
	}
    */
}
